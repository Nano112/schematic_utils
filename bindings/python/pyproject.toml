[build-system]
requires = ["scikit-build-core >=0.10", "nanobind >=2.12,<3"]
build-backend = "scikit_build_core.build"

[project]
name = "nucleation"
version = "0.10.24"
description = "A high-performance Minecraft schematic parser and utility library (generated bindings)"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Nano", email = "nano@schem.at" }]

[project.urls]
Homepage = "https://github.com/Schem-at/Nucleation"

[tool.scikit-build]
minimum-version = "0.10"
build-dir = "build/{wheel_tag}"
# The hand-written veneer package; the compiled core is installed into it
# by CMake as the `nucleation.nucleation` submodule.
wheel.packages = ["nucleation"]
# Stable ABI wheels for CPython 3.12+
wheel.py-api = "cp312"
# Populated only for source-distribution builds. Local wheels continue to use
# the repository root, while published sdists carry the complete Rust core.
sdist.include = ["rust/**", "normalize_stub.py"]
